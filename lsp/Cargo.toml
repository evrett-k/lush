[package]
name = "lush-lsp"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1.0", features = ["full"] }
tower-lsp = "0.20.0"
mlua = { version = "0.11.6", features = ["lua54", "vendored"] }
lush = { path = ".." }